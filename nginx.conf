server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: react-router 深連結都回 index.html
  location / {
    try_files $uri $uri/ /index.html;
  }

  # 靜態資源長快取
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }
}
